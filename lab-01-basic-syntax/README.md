# lab-01-basic-syntax

**变量类型、切片操作（Slicing）与列表推导式**

## 知识点

- Python 变量与类型（动态类型，无需声明）
- 切片 `[start:stop:step]`：字符串、列表、元组
- 列表推导式 `[x for x in ... if ...]`、字典推导式、集合推导式

## 运行

```bash
python main.py
```
