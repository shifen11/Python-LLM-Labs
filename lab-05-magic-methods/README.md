# lab-05-magic-methods

**魔术方法（协议）**

## 知识点

- `__init__`：构造；`__str__` / `__repr__`：字符串表示
- `__call__`：实例可被调用，像函数一样
- `__len__`、`__getitem__`：支持 `len(obj)`、`obj[i]`
- Python 的「协议」：实现特定方法即支持相应语法，无需继承

## 运行

```bash
python main.py
```
